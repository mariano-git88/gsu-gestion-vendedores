streamlit>=1.40
# starlette 1.4.0 agregó el argumento obligatorio thread_minimum_size a
# GZipResponder y streamlit 1.61.0 (la última) todavía no se lo pasa: el server
# devuelve 500 en todas las requests. Cuando streamlit lo soporte, sacar el tope.
starlette<1.4
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
requests>=2.31
gspread>=6.0
google-auth>=2.30
anthropic>=0.40
pypdf>=4.0
reportlab>=4.0
rapidfuzz>=3.0
